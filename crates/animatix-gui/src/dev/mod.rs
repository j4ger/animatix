//! Development-only tools for visual inspection of UI components.
//!
//! This module is compiled only when the `dev-screenshots` feature is enabled.
//! It must never be used in shipped binaries.

pub mod screenshot_harness;
