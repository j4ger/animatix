- 在 `grammar.js` 中广泛使用 `field()` 包装器标记 AST 节点的关键组成部分（如 `name`, `value`），以提升语法树的可读性与查询效率。
- 通过 `prec.left()` 和 `prec()` 显式声明二元运算符及函数调用的优先级，解决表达式解析中的冲突问题。
- 遵循 Tree-sitter 命名规范，以下划线前缀（如 `_statement`, `_expression`）定义中间抽象节点，避免其直接出现在最终的语法树叶子节点中。