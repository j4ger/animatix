- **核心定义层**：`grammar.js` 定义了语言的抽象语法树（AST）结构，涵盖场景声明、关键帧、动作调用及控制流等语句。
- **词法扩展层**：`src/scanner.c` 实现外部扫描器，专门处理数字与时间字面量（如 `800ms`）的歧义消除。
- **语义增强层**：`queries/highlights.scm` 基于 AST 节点映射编辑器语法高亮规则，区分关键字、类型及变量作用域。
- **验证体系**：`test/corpus/` 目录通过文本语料覆盖各类语法分支，确保解析器的健壮性。