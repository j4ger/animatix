- 所有编辑操作均遵循“查找-修改-返回 Result”的模式，失败时返回包含上下文信息的 `SourceEditError`。
- 在处理时间轴逻辑时，统一在绝对时间（Seconds）与相对偏移（RelativeKeyframe）之间进行动态转换与补偿，以维持后续关键帧的绝对时间点不变。
- 属性名在内部处理时通过 `canonical_to_source` 进行规范化映射（如将 UI 层的 'position' 映射为源码层的 'at'）。