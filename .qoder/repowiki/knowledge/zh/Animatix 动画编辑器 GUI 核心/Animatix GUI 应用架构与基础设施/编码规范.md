- 采用命令-效果（Command-Effect）模式处理用户交互，UI 层产生 `ActionQueue`，由 Shell 统一执行并应用副作用。
- 在 `DocumentSession` 中利用哈希比对（`last_source_hash`）跳过未变更源码的重建过程，提升响应速度。
- 所有结构化错误均通过 `GuiError` 枚举统一表示，并利用 `thiserror` 自动实现 `std::fmt::Display`。
- 预览渲染采用“最后成功快照”（last-good snapshot）机制，在编译失败时保持画面不闪烁。