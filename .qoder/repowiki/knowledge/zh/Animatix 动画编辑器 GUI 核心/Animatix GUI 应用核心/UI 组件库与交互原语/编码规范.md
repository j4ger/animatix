- 所有组件文件均通过 `use crate::app::design_tokens::*;` 导入设计系统，禁止在组件内硬编码颜色或尺寸值。
- 复杂交互组件普遍采用 Builder 模式（如 `Row::new().icon().show()`），通过链式调用配置属性并返回响应结构体。
- 自定义绘制逻辑统一使用 `ui.painter()` 进行底层图形渲染，以实现对控件外观的像素级控制。
- 交互元素的状态反馈（如悬停、选中、激活）严格遵循设计令牌定义的语义化颜色（如 `BG_HOVER`, `ACCENT_BLUE`）。