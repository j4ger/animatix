- 采用“上下文对象”模式，每个面板的入口函数接收一个包含所有必要引用（状态、命令队列、资源）的 Context 结构体。
- 使用 `panel_frame()` 统一面板的内边距和背景样式，确保UI视觉的一致性。
- 通过 `Command` 枚举解耦UI事件与业务逻辑，面板仅负责收集用户输入并推送到 `ActionQueue`。
- 在 `inspector` 中采用“语义/强度/表格”多视图模式切换，以适应不同复杂度的属性编辑需求。