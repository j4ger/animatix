- 模块由 `mod.rs` 统一导出，按领域拆分为 `actor`、`file`、`keyframe`、`playback`、`property`、`scene`、`ui` 等子模块。
- 采用纯函数风格：每个 `handle_*` 接收所需 Store 的可变或共享引用，不依赖全局状态或完整 Shell。
- 通过 `DocumentController` 封装复杂业务逻辑，Handler 负责快照与委托，保持调度层轻薄。
- 返回值统一为 `Vec<Effect>`，将重绘、状态提示等副作用交由外层 Shell 执行，实现逻辑与 UI 渲染解耦。