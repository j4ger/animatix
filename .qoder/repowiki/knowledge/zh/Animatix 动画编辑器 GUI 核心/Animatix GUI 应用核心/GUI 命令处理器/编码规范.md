- 函数命名遵循 `handle_<noun>_<verb>` 模式（如 `handle_toggle_playback`）。
- 所有处理函数返回 `Vec<Effect>` 以声明式描述需要执行的副作用。
- 涉及状态变更的处理函数在修改前调用 `document_store.snapshot()` 以支持撤销操作。
- 处理函数仅接收其实际需要的 Store 引用，只读 Store 使用共享引用，可变 Store 使用可变引用。