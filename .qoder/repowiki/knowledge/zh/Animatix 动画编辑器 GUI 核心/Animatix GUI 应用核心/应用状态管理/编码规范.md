- Store 结构体通常包含多个子状态模块（如 `UiStore` 包含 `SelectionStore`、`InteractionStore`），并通过 `new` 构造函数进行初始化。
- 涉及源文本变更的操作（如 `commit_source`、`replace_text`）会自动触发 `source_epoch` 递增并标记当前快照为过时（Stale）。
- 使用 `Arc<DocumentSnapshot>` 存储不可变的历史快照，通过 `current` 和 `last_good` 双指针机制支持构建失败时的回退显示。