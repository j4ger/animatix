- 在 `EditorBuffer` 的多个 impl 块中，通过 `self.cell_index_for_source_line` 和 `source_line_for_cell` 实现文档全局行号与单元格内部相对行号的频繁转换。
- 在处理单元格头部（如关键帧时间戳）与主体内容时，统一采用计算 `header_lines` 偏移量的方式来确定诊断信息和语法高亮的实际作用范围。
- 所有涉及源码修改的操作（如插入补全、单元格增删）均会触发 `self.analyzer.update(&self.text)` 并清除 `cached_highlight`，以确保分析状态与 UI 显示的强一致性。