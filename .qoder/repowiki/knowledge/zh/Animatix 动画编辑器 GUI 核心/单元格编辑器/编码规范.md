- 采用“待处理操作”（Pending Actions）模式，UI 交互仅修改 `CellEditorState` 中的 `pending_*` 字段，由外部调用者执行实际的数据变更。
- 在渲染循环中实现高亮布局（LayoutJob）缓存机制，仅在单元格内容变更时重新触发 `highlight_source` 以提升性能。
- 统一使用 `CellDiagnostic` 结构体将分析器诊断映射到单元格内部的相对行列坐标，并在渲染层通过波浪线可视化错误与警告。