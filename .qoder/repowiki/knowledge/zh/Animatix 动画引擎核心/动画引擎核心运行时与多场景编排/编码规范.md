- 采用“构建时编译-运行时执行”的双阶段架构，将 AST 在 `build` 阶段降阶为 IR 或字节码，在 `evaluate` 阶段通过 VM 或解释器执行。
- 使用 `BuildReport<T>` 模式统一返回构建结果与诊断信息，确保错误和警告能随产物一起传递。
- 在 `composition` 中通过 `BTreeMap` 维护场景顺序与边关系，利用其有序性简化全局时间线的线性扫描与映射。