- 采用零尺寸结构体配合 `pub const` 常量（如 `RECT`, `TEXT`）实现单例模式，避免运行时堆分配。
- 所有原语实现均遵循“元数据声明 + 核心逻辑实现”的结构，通过 `type_name` 和 `kind_id` 与引擎其他模块进行强类型绑定。
- 在 `evaluate` 方法中统一使用辅助函数（如 `evaluate_shape_render`）处理样式采样与状态转换，减少重复代码。