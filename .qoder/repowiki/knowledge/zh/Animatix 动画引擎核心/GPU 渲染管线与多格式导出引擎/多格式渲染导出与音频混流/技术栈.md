- **视频编码**：使用 `rsmpeg` (v0.18.0) 绑定 FFmpeg 7.1，支持 libx264、h264_nvenc、h264_vaapi 及 libvpx-vp9 编码器。
- **图像处理**：使用 `image` crate (v0.25.10) 处理 PNG 保存及 GIF 帧编码。
- **异步桥接**：使用 `pollster` 在同步导出接口中阻塞执行异步渲染管道。