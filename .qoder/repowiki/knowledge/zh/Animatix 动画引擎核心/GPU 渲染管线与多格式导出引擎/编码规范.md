- 所有 GPU 资源创建与渲染指令提交均通过 `wgpu::Device` 与 `wgpu::Queue` 显式管理，确保线程安全与资源隔离。
- 渲染入口统一支持 `DebugRenderOptions`，允许在评估时间轴时注入调试可视化信息而不改变核心渲染逻辑。
- 错误处理统一使用自定义 `RenderError` 或 `ExportError`，并将底层 WGPU/Vello/FFmpeg 错误转换为语义化的字符串描述。