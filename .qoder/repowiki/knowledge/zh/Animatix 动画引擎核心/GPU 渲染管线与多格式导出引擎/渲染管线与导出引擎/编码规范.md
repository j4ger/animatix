- 所有 GPU 资源初始化（Device/Adapter）均采用异步创建后通过 pollster::block_on 转为同步接口的模式。
- 渲染目标（Texture/View）采用懒加载与尺寸缓存机制（ensure_targets），仅在分辨率变更时重建 GPU 资源。
- 错误处理统一使用自定义枚举（RenderError/ExportError），并通过 map_err 将底层 wgpu/vello 错误转换为语义化字符串。
- 在涉及多场景过渡渲染时，通过显式作用域控制 vello::Scene 的生命周期，以避免同时持有多个大型场景对象导致内存峰值过高。