- 表达式编译采用递归下降模式，将 AST 节点映射为 `CompiledExpr` 枚举变体。
- 内置函数（如 `sin`, `lerp`）在 `BuiltinFn` 枚举中定义，并在求值时通过模式匹配分发。
- 向量构造遵循长度推断规则：2/3/4 个元素分别自动转换为 `Vec2`/`Vec3`/`Vec4`，其余为 `List`。
- 二进制运算支持标量与向量的广播机制（如 `Num` 与 `Vec3` 运算），并封装了安全除法和取余逻辑。