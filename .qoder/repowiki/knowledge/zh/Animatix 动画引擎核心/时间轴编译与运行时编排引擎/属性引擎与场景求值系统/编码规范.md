- 所有属性读写操作必须通过 `ActorField` 枚举和 `TrackAccessor` trait 进行统一分发，禁止直接访问 `AnimationTrack` 的具体字段。
- 关键帧插值逻辑统一实现在 `PropertyTrack<T>` 的 `evaluate` 方法中，并通过 `last_evaluated` 缓存实现单帧内的时序一致性优化。
- 运行时环境注入遵循 `inject_property_into_env` 规范，自动将属性值及其子分量（如 `.x`, `.r`）映射到 `Environment` 中以供表达式求值。