- 所有动作实现均遵循“解析修饰符 -> 计算起止时间 -> 遍历目标 -> 插入关键帧”的标准执行流程。
- 在处理带有延迟的瞬时动作（duration=0）时，普遍采用 `ensure_guard_keyframe` 机制在起始前一毫秒插入保护帧以维持状态连续性。
- 动作参数解析统一使用辅助函数（如 `parse_vec2_modifier`）并结合 `Diagnostic` 系统收集冲突或缺失参数的警告信息。