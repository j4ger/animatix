- 所有公共 API 均采用纯函数或无 I/O 设计，输入为 `&str` 或 AST 数据，输出为结构化结果，确保分析器可在任意环境中复用。
- 位置信息统一采用 1-based 的行/列坐标存储在符号表中，而在与 Tree-sitter 交互或生成 LSP 诊断时转换为 0-based 坐标。
- 利用 `OnceLock` 缓存静态元数据（如内置类型、属性映射），避免在每次分析过程中重复构建常量集合。
- 诊断系统支持通过源码注释（`// lint-disable: ...`）或 `.amx.toml` 文件动态配置 Lint 规则，并在收集阶段进行过滤。