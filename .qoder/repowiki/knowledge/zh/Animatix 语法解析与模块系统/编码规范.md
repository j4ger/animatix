- AST 匹配穷尽性通过 `format_core.rs` 中的计数断言测试（如 `format_expr_covers_all_expr_variants`）进行强制约束。
- 遍历逻辑在 `walk.rs` 中统一实现不可变与可变双版本（如 `walk_stmts` 与 `walk_stmts_mut`），确保状态修改与只读访问的模式一致性。
- 诊断信息采用链式调用构建（如 `Diagnostic::error(...).with_path(...).with_location(...)`），以灵活附加源码位置与上下文。
- 解析入口统一经过 `strip_comments` 预处理，将行注释替换为空格以保留字节偏移，确保错误定位与原始源码对齐。